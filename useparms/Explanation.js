1. Using location to get current data

 How to use ==> 
useNavigate() = navigate ( "pathname" ,{state: we can send the data})    in that pathname where we can get the data by using uselocation method 

2. Using useParams to get current I'd of the data and  we can filter it after

How to use => 
useNavigate() = navigate("pathname when using use params we want I'd for example  "/card/:id" )
Using I'd we can filter the array and get the exact I'd of the card (note: use params I'd is string)
